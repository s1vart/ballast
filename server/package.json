{
  "name": "ballast-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ballast backend — holds Plaid secrets + access tokens (encrypted at rest), exchanges tokens, returns balances/transactions.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "plaid": "^43.0.0"
  }
}
