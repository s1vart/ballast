{
  "name": "ballast-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ballast backend — holds Plaid secrets + access tokens, exchanges tokens, returns balances/transactions.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "plaid": "latest"
  }
}
